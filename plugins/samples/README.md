# Runtime Samples

This folder contains runnable sample plugin payloads for quick local verification.

- `beat_normalizer/`: basic process plugin with tool-action + file mutation flow.
- `note_chain_interaction_demo/`: Host API v3 interactive canvas sample (`canvas_interaction`, `panel_workspace`).

